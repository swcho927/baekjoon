// ════════════════════════════════════════════════════════
//  problems/1000.js  —  문제 번호 1000: A+B
//
//  이 파일 하나가 문제 하나를 정의합니다.
//  새 문제를 추가하려면 이 파일을 복사해서 번호만 바꾸면 됩니다.
// ════════════════════════════════════════════════════════

const PROBLEM_1000 = {

    // ── 기본 정보 ──────────────────────────────────────
    id:    1000,
    title: "A+B",

    // ── 제한 정보 ──────────────────────────────────────
    timeLimit:   2,      // 초
    memoryLimit: 256,    // MB

    // ── 문제 본문 (HTML 허용) ───────────────────────────
    description: `두 정수 A와 B를 입력받은 다음, A+B를 출력하는 프로그램을 작성하시오.`,

    // ── 입력 형식 ──────────────────────────────────────
    inputDesc: `첫째 줄에 A와 B가 주어진다. (0 &lt; A, B &lt; 10)`,

    // ── 출력 형식 ──────────────────────────────────────
    outputDesc: `첫째 줄에 A+B를 출력한다.`,

    // ── 예제 (사용자에게 보여주는 용도) ────────────────
    examples: [
        { input: "1 2",  output: "3"  },
        { input: "3 7",  output: "10" },
    ],

    // ── 실제 채점용 테스트케이스 (사용자에게 숨김) ──────
    //  { in: "입력", out: "정답" } 형태
    testCases: [
        { in: "1 2",     out: "3"   },
        { in: "10 20",   out: "30"  },
        { in: "100 200", out: "300" },
        { in: "0 0",     out: "0"   },
        { in: "-5 5",    out: "0"   },
        { in: "7 3",     out: "10"  },
    ],

    // ── 에디터에 처음 보여줄 기본 코드 ─────────────────
    starterCode: `import sys

# 한 줄을 읽어서 두 숫자로 나누기
a, b = map(int, sys.stdin.readline().split())
print(a + b)`,
};
